({
/*
    testNotStorable: {
        test : [
            function(cmp, helper) {
                var action = cmp.get("c.notStorable");
                $A.test.assertFalsy(action.isStorable(), "Expected storable to be false");
            }
        ]
    },

    testStorable: {
        test : [
            function(cmp, helper) {
                var action = cmp.get("c.storable");
                $A.test.assertTruthy(action.isStorable(), "Expected storable to be true");
            }
        ]
    }
*/
})
