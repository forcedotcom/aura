/**
 * these are some comments in the file
 */

//#mock {"modes": ["MOCK1"]}
var stuff = {
    i: "am string",

    numeros: 23525,

    //#mock {"modes": ["MOCK2"]}

    blah: function(food) {
        return "i like to eat " + food;
    }
};
